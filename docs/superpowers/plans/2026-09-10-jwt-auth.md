# JWT 登录认证实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现管理员登录（POST /auth/login），签发 JWT；给 /admin/* 路由组挂鉴权中间件，无有效 token 一律 401；提供初始管理员种子脚本。

**Architecture:** service/auth.go 负责用户验证和 JWT 签发；middleware/jwt.go 负责每次请求的 token 校验；handler/auth.go 处理登录 HTTP 请求。JWT secret 和有效期从 config.yaml 读取，通过依赖注入传到各层，不使用全局变量。

**Tech Stack:** github.com/golang-jwt/jwt/v5，golang.org/x/crypto/bcrypt（已在 go.sum，需拉成 direct），标准库 net/http。

## Global Constraints

- 模块路径：`abingblog-backend`（不要改动）
- Go 版本：1.27.1
- JWT 算法：HS256
- 密码哈希：bcrypt cost=12
- Token 有效期：从 config 读取，单位秒，默认 86400（1 天）
- 错误码：与 errcode/errcode.go 和 docs/api-v1.md 保持一致（CodeLoginFail=1001）
- HTTP 状态码：401 表示未认证，200 业务错误用 code 字段区分
- 不要引入新的第三方框架（只加 jwt/v5 这一个库）

---

## 文件变更地图

| 操作 | 文件 | 职责 |
|---|---|---|
| **修改** | `internal/service/article.go` | 修复 `new(time.Now())` 编译错误（3 处） |
| **新建** | `internal/repository/user.go` | 按 username 查用户；创建用户 |
| **新建** | `internal/service/auth.go` | bcrypt 验密 + JWT 签发逻辑 |
| **新建** | `internal/handler/auth.go` | POST /auth/login HTTP 处理器 |
| **修改** | `internal/handler/handler.go` | 加入 `Auth *AuthHandler` 字段 |
| **新建** | `internal/middleware/jwt.go` | JWTAuth() gin.HandlerFunc，校验 Bearer token |
| **修改** | `internal/config/config.go` | Config 加 JWTConfig 字段 |
| **修改** | `configs/config.yaml` | 加 jwt.secret / jwt.expires_in |
| **修改** | `internal/router/router.go` | 注册 /auth/login；admin 组挂 JWTAuth |
| **修改** | `cmd/server/main.go` | 组装 AuthHandler，传 jwtSecret 给中间件 |
| **新建** | `cmd/seed/main.go` | 一次性脚本：bcrypt 哈希密码并写入 users 表 |

---

### Task 0: 修复编译错误

**Files:**
- Modify: `internal/service/article.go:124`, `internal/service/article.go:162`, `internal/service/article.go:204`

**Interfaces:**
- Consumes: 无
- Produces: 编译通过的 article.go（后续所有任务的前提）

- [ ] **Step 1: 修改第 124 行**

将：
```go
a.PublishedAt = new(time.Now())
```
改为：
```go
t := time.Now()
a.PublishedAt = &t
```

- [ ] **Step 2: 修改第 162 行**

将：
```go
a.PublishedAt = new(time.Now())
```
改为：
```go
t := time.Now()
a.PublishedAt = &t
```

- [ ] **Step 3: 修改第 204 行**

将：
```go
publishedAt = new(time.Now())
```
改为：
```go
t := time.Now()
publishedAt = &t
```

- [ ] **Step 4: 验证编译**

```bash
go build ./...
```
期望：无输出（无错误）

- [ ] **Step 5: Commit**

```bash
git add internal/service/article.go
git commit -m "fix: 修复 new(time.Now()) 编译错误，改为取地址"
```

---

### Task 1: 添加 JWT 依赖

**Files:**
- Modify: `go.mod`, `go.sum`

**Interfaces:**
- Consumes: 无
- Produces: `github.com/golang-jwt/jwt/v5` 可 import

- [ ] **Step 1: 拉取依赖**

```bash
go get github.com/golang-jwt/jwt/v5@v5.2.2
```
期望：go.mod 出现 `github.com/golang-jwt/jwt/v5 v5.2.2`

- [ ] **Step 2: 整理依赖**

```bash
go mod tidy
```

- [ ] **Step 3: 验证编译**

```bash
go build ./...
```
期望：无输出

- [ ] **Step 4: Commit**

```bash
git add go.mod go.sum
git commit -m "chore: 添加 golang-jwt/jwt/v5 依赖"
```

---

### Task 2: 扩展 Config，加 JWT 配置

**Files:**
- Modify: `internal/config/config.go`
- Modify: `configs/config.yaml`

**Interfaces:**
- Consumes: 无
- Produces: `cfg.JWT.Secret string`，`cfg.JWT.ExpiresIn int`（秒）

- [ ] **Step 1: 修改 config.go，加 JWTConfig 结构体**

在 `ServerConfig` 定义下方加：
```go
// JWTConfig JWT 签发配置
type JWTConfig struct {
	Secret    string `mapstructure:"secret"`
	ExpiresIn int    `mapstructure:"expires_in"` // 单位：秒
}
```

同时在 `Config` 结构体里加字段：
```go
type Config struct {
	Server ServerConfig `mapstructure:"server"`
	MySQL  MySQLConfig  `mapstructure:"mysql"`
	JWT    JWTConfig    `mapstructure:"jwt"`
}
```

- [ ] **Step 2: 修改 config.yaml，加 jwt 节**

在文件末尾追加：
```yaml
jwt:
  secret: "change-me-in-production"   # 生产环境通过环境变量覆盖
  expires_in: 86400                    # 1 天，单位秒
```

- [ ] **Step 3: 验证编译**

```bash
go build ./...
```

- [ ] **Step 4: Commit**

```bash
git add internal/config/config.go configs/config.yaml
git commit -m "feat: config 加 JWT secret/expires_in 配置项"
```

---

### Task 3: 新建 UserRepo

**Files:**
- Create: `internal/repository/user.go`

**Interfaces:**
- Consumes: `*gorm.DB`，`model.User`（已有）
- Produces:
  - `NewUserRepo(db *gorm.DB) *UserRepo`
  - `(r *UserRepo) GetByUsername(username string) (*model.User, error)` — 查不到返回 `nil, nil`
  - `(r *UserRepo) Create(u *model.User) error`

- [ ] **Step 1: 创建文件**

```go
package repository

import (
	"abingblog-backend/internal/model"
	"gorm.io/gorm"
)

type UserRepo struct {
	db *gorm.DB
}

func NewUserRepo(db *gorm.DB) *UserRepo {
	return &UserRepo{db: db}
}

// GetByUsername 按用户名查用户；不存在返回 nil, nil
func (r *UserRepo) GetByUsername(username string) (*model.User, error) {
	var u model.User
	err := r.db.Where("username = ?", username).First(&u).Error
	if err == gorm.ErrRecordNotFound {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return &u, nil
}

// Create 创建用户
func (r *UserRepo) Create(u *model.User) error {
	return r.db.Create(u).Error
}
```

- [ ] **Step 2: 验证编译**

```bash
go build ./...
```

- [ ] **Step 3: Commit**

```bash
git add internal/repository/user.go
git commit -m "feat: 新增 UserRepo（GetByUsername / Create）"
```

---

### Task 4: 新建 AuthService

**Files:**
- Create: `internal/service/auth.go`

**Interfaces:**
- Consumes:
  - `*repository.UserRepo`（来自 Task 3）
  - `cfg.JWT.Secret string`，`cfg.JWT.ExpiresIn int`（来自 Task 2）
  - `github.com/golang-jwt/jwt/v5`（来自 Task 1）
  - `golang.org/x/crypto/bcrypt`
- Produces:
  - `NewAuthService(userRepo *repository.UserRepo, secret string, expiresIn int) *AuthService`
  - `(s *AuthService) Login(username, password string) (token string, expiresIn int, err error)` — 失败返回 errcode.CodeLoginFail

- [ ] **Step 1: 创建文件**

```go
package service

import (
	"time"

	"abingblog-backend/internal/errcode"
	"abingblog-backend/internal/repository"

	"github.com/golang-jwt/jwt/v5"
	"golang.org/x/crypto/bcrypt"
)

// AuthService 负责登录验证和 JWT 签发
type AuthService struct {
	userRepo  *repository.UserRepo
	secret    []byte
	expiresIn int // 秒
}

func NewAuthService(userRepo *repository.UserRepo, secret string, expiresIn int) *AuthService {
	if expiresIn <= 0 {
		expiresIn = 86400
	}
	return &AuthService{userRepo: userRepo, secret: []byte(secret), expiresIn: expiresIn}
}

// Login 验证用户名/密码，成功返回 JWT token 和有效期（秒）
func (s *AuthService) Login(username, password string) (string, int, error) {
	u, err := s.userRepo.GetByUsername(username)
	if err != nil {
		return "", 0, err
	}
	if u == nil {
		return "", 0, errcode.New(errcode.CodeLoginFail, "用户名或密码错误")
	}
	if err := bcrypt.CompareHashAndPassword([]byte(u.Password), []byte(password)); err != nil {
		return "", 0, errcode.New(errcode.CodeLoginFail, "用户名或密码错误")
	}

	now := time.Now()
	claims := jwt.RegisteredClaims{
		Subject:   u.Username,
		IssuedAt:  jwt.NewNumericDate(now),
		ExpiresAt: jwt.NewNumericDate(now.Add(time.Duration(s.expiresIn) * time.Second)),
	}
	token, err := jwt.NewWithClaims(jwt.SigningMethodHS256, claims).SignedString(s.secret)
	if err != nil {
		return "", 0, err
	}
	return token, s.expiresIn, nil
}
```

- [ ] **Step 2: 验证编译**

```bash
go build ./...
```

- [ ] **Step 3: Commit**

```bash
git add internal/service/auth.go
git commit -m "feat: 新增 AuthService（bcrypt 验密 + JWT 签发）"
```

---

### Task 5: 新建 AuthHandler，更新 Handler 聚合

**Files:**
- Create: `internal/handler/auth.go`
- Modify: `internal/handler/handler.go`

**Interfaces:**
- Consumes: `*service.AuthService`（来自 Task 4）
- Produces:
  - `NewAuthHandler(svc *service.AuthService) *AuthHandler`
  - `(h *AuthHandler) Login(c *gin.Context)` — 读 JSON body `{username, password}` → 调 svc.Login → 返回 `{token, expires_in}`

- [ ] **Step 1: 创建 handler/auth.go**

```go
package handler

import (
	"net/http"

	"abingblog-backend/internal/errcode"
	"abingblog-backend/internal/response"
	"abingblog-backend/internal/service"

	"github.com/gin-gonic/gin"
)

// AuthHandler 认证接口层
type AuthHandler struct {
	svc *service.AuthService
}

func NewAuthHandler(svc *service.AuthService) *AuthHandler {
	return &AuthHandler{svc: svc}
}

// Login POST /auth/login
func (h *AuthHandler) Login(c *gin.Context) {
	var req struct {
		Username string `json:"username"`
		Password string `json:"password"`
	}
	if err := c.ShouldBindJSON(&req); err != nil {
		response.Fail(c, http.StatusBadRequest, errcode.CodeBadParam, "请求参数错误")
		return
	}
	if req.Username == "" || req.Password == "" {
		response.Fail(c, http.StatusBadRequest, errcode.CodeBadParam, "用户名和密码不能为空")
		return
	}
	token, expiresIn, err := h.svc.Login(req.Username, req.Password)
	if err != nil {
		response.Error(c, err)
		return
	}
	response.OK(c, gin.H{"token": token, "expires_in": expiresIn})
}
```

- [ ] **Step 2: 修改 handler/handler.go，加 Auth 字段**

将文件内容改为：
```go
package handler

// Handler 聚合所有接口处理器，由 main 统一组装依赖后传给 router
type Handler struct {
	Article  *ArticleHandler
	Category *CategoryHandler
	Tag      *TagHandler
	Health   *HealthHandler
	Auth     *AuthHandler
}
```

- [ ] **Step 3: 验证编译**

```bash
go build ./...
```

- [ ] **Step 4: Commit**

```bash
git add internal/handler/auth.go internal/handler/handler.go
git commit -m "feat: 新增 AuthHandler（POST /auth/login）"
```

---

### Task 6: 新建 JWT 鉴权中间件

**Files:**
- Create: `internal/middleware/jwt.go`

**Interfaces:**
- Consumes: `jwtSecret string`，`github.com/golang-jwt/jwt/v5`
- Produces: `JWTAuth(secret string) gin.HandlerFunc` — 无效/缺失 token 直接 Abort 401，有效则 Next

- [ ] **Step 1: 创建文件**

```go
package middleware

import (
	"net/http"
	"strings"

	"github.com/gin-gonic/gin"
	"github.com/golang-jwt/jwt/v5"
)

// JWTAuth 校验 Authorization: Bearer <token>；无效则 401 中止
func JWTAuth(secret string) gin.HandlerFunc {
	key := []byte(secret)
	return func(c *gin.Context) {
		auth := c.GetHeader("Authorization")
		if !strings.HasPrefix(auth, "Bearer ") {
			c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{
				"code":    1401,
				"message": "未提供有效的认证 token",
				"data":    nil,
			})
			return
		}
		tokenStr := strings.TrimPrefix(auth, "Bearer ")
		_, err := jwt.Parse(tokenStr, func(t *jwt.Token) (any, error) {
			if _, ok := t.Method.(*jwt.SigningMethodHMAC); !ok {
				return nil, jwt.ErrSignatureInvalid
			}
			return key, nil
		})
		if err != nil {
			c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{
				"code":    1401,
				"message": "token 无效或已过期",
				"data":    nil,
			})
			return
		}
		c.Next()
	}
}
```

- [ ] **Step 2: 验证编译**

```bash
go build ./...
```

- [ ] **Step 3: Commit**

```bash
git add internal/middleware/jwt.go
git commit -m "feat: 新增 JWTAuth 中间件（Bearer token 校验）"
```

---

### Task 7: 更新 Router 和 main.go

**Files:**
- Modify: `internal/router/router.go`
- Modify: `cmd/server/main.go`

**Interfaces:**
- Consumes:
  - `h.Auth *AuthHandler`（来自 Task 5）
  - `middleware.JWTAuth(secret)`（来自 Task 6）
  - `cfg.JWT.Secret`（来自 Task 2）
- Produces: POST /auth/login 可调用；/admin/* 无 token 返回 401

- [ ] **Step 1: 修改 router/router.go**

将文件完整替换为：
```go
package router

import (
	"abingblog-backend/internal/handler"
	"abingblog-backend/internal/middleware"

	"github.com/gin-gonic/gin"
)

// Setup 注册所有路由。路由分组即权限边界：
//   - /api/v1         公共接口：游客可用，只暴露已发布内容
//   - /api/v1/admin   后台接口：须携带有效 JWT（Authorization: Bearer <token>）
func Setup(h *handler.Handler, jwtSecret string) *gin.Engine {
	r := gin.Default()

	v1 := r.Group("/api/v1")
	{
		v1.POST("/auth/login", h.Auth.Login)

		v1.GET("/articles", h.Article.List)
		v1.GET("/articles/:id", h.Article.Get)
		v1.GET("/categories", h.Category.List)
		v1.GET("/tags", h.Tag.List)
		v1.GET("/healthz", h.Health.Check)

		admin := v1.Group("/admin")
		admin.Use(middleware.JWTAuth(jwtSecret))
		{
			admin.GET("/articles", h.Article.AdminList)
			admin.POST("/articles", h.Article.Create)
			admin.PUT("/articles/:id", h.Article.Update)
			admin.DELETE("/articles/:id", h.Article.Delete)
			admin.PUT("/articles/:id/status", h.Article.UpdateStatus)

			admin.POST("/categories", h.Category.Create)
			admin.PUT("/categories/:id", h.Category.Update)
			admin.DELETE("/categories/:id", h.Category.Delete)

			admin.POST("/tags", h.Tag.Create)
			admin.PUT("/tags/:id", h.Tag.Update)
			admin.DELETE("/tags/:id", h.Tag.Delete)
		}
	}
	return r
}
```

- [ ] **Step 2: 修改 cmd/server/main.go**

将文件完整替换为：
```go
package main

import (
	"fmt"
	"log"

	"abingblog-backend/internal/config"
	"abingblog-backend/internal/database"
	"abingblog-backend/internal/handler"
	"abingblog-backend/internal/repository"
	"abingblog-backend/internal/router"
	"abingblog-backend/internal/service"
)

func main() {
	cfg := config.Load()
	db := database.Init(cfg)

	articleRepo := repository.NewArticleRepo(db)
	categoryRepo := repository.NewCategoryRepo(db)
	tagRepo := repository.NewTagRepo(db)
	userRepo := repository.NewUserRepo(db)

	h := &handler.Handler{
		Article:  handler.NewArticleHandler(service.NewArticleService(articleRepo, categoryRepo, tagRepo)),
		Category: handler.NewCategoryHandler(service.NewCategoryService(categoryRepo)),
		Tag:      handler.NewTagHandler(service.NewTagService(tagRepo)),
		Health:   handler.NewHealthHandler(db),
		Auth:     handler.NewAuthHandler(service.NewAuthService(userRepo, cfg.JWT.Secret, cfg.JWT.ExpiresIn)),
	}

	r := router.Setup(h, cfg.JWT.Secret)
	log.Printf("server running at http://localhost:%d", cfg.Server.Port)
	if err := r.Run(fmt.Sprintf(":%d", cfg.Server.Port)); err != nil {
		log.Fatal(err)
	}
}
```

- [ ] **Step 3: 验证编译**

```bash
go build ./...
```
期望：无输出

- [ ] **Step 4: Commit**

```bash
git add internal/router/router.go cmd/server/main.go
git commit -m "feat: router 挂 JWTAuth 中间件，注册 /auth/login 路由"
```

---

### Task 8: 种子脚本（初始管理员）

**Files:**
- Create: `cmd/seed/main.go`

**Interfaces:**
- Consumes: `config.Load()`，`database.Init()`，`repository.NewUserRepo()`，`golang.org/x/crypto/bcrypt`
- Produces: 向 users 表插入一条管理员记录（username=admin，password 为 bcrypt 哈希）

- [ ] **Step 1: 创建文件**

```go
package main

import (
	"fmt"
	"log"

	"abingblog-backend/internal/config"
	"abingblog-backend/internal/database"
	"abingblog-backend/internal/model"
	"abingblog-backend/internal/repository"

	"golang.org/x/crypto/bcrypt"
)

func main() {
	cfg := config.Load()
	db := database.Init(cfg)
	userRepo := repository.NewUserRepo(db)

	existing, err := userRepo.GetByUsername("admin")
	if err != nil {
		log.Fatalf("查询用户失败: %v", err)
	}
	if existing != nil {
		fmt.Println("admin 用户已存在，跳过创建")
		return
	}

	hash, err := bcrypt.GenerateFromPassword([]byte("admin123"), 12)
	if err != nil {
		log.Fatalf("生成密码哈希失败: %v", err)
	}
	u := &model.User{
		Username: "admin",
		Password: string(hash),
	}
	if err := userRepo.Create(u); err != nil {
		log.Fatalf("创建用户失败: %v", err)
	}
	fmt.Printf("admin 用户创建成功（id=%d），密码：admin123\n", u.ID)
	fmt.Println("生产环境请立即通过数据库修改密码！")
}
```

- [ ] **Step 2: 验证编译**

```bash
go build ./cmd/seed/...
```

- [ ] **Step 3: 运行种子（需要 MySQL 在线）**

```bash
go run ./cmd/seed/main.go
```
期望输出：
```
admin 用户创建成功（id=1），密码：admin123
生产环境请立即通过数据库修改密码！
```

- [ ] **Step 4: Commit**

```bash
git add cmd/seed/main.go
git commit -m "feat: 添加初始管理员种子脚本 cmd/seed"
```

---

### Task 9: 端到端冒烟测试

无需写单元测试文件，用 curl 手动验证整条链路。

**前提：** MySQL 在线，已运行过 `go run ./cmd/seed/main.go`

- [ ] **Step 1: 启动服务**

```bash
go run ./cmd/server/main.go
```

- [ ] **Step 2: 测试登录成功**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python -m json.tool
```
期望：`code=0`，`data.token` 非空，`data.expires_in=86400`

- [ ] **Step 3: 无 token 访问后台，期望 401**

```bash
curl -s http://localhost:8080/api/v1/admin/articles | python -m json.tool
```
期望：HTTP 401，`code=1401`

- [ ] **Step 4: 带 token 访问后台，期望 200**

```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")

curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/admin/articles | python -m json.tool
```
期望：`code=0`，`data.list` 数组

- [ ] **Step 5: 密码错误，期望 401**

```bash
curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"wrong"}' | python -m json.tool
```
期望：HTTP 401，`code=1001`

- [ ] **Step 6: 最终 Commit（如有遗漏文件）**

```bash
git status
# 确认无未提交文件后结束
```

---

## 自检

| 需求 | 覆盖 Task |
|---|---|
| 编译错误修复 | Task 0 |
| JWT 依赖 | Task 1 |
| JWT 配置（secret/expires_in） | Task 2 |
| UserRepo | Task 3 |
| bcrypt 验密 + JWT 签发 | Task 4 |
| POST /auth/login 接口 | Task 5 |
| Bearer token 中间件 | Task 6 |
| /admin/* 挂鉴权 + 路由注册 | Task 7 |
| 初始管理员脚本 | Task 8 |
| 端到端冒烟验证 | Task 9 |
| 错误码 1001（登录失败）映射为 401 | Task 4（service 抛）+ response.go（已有映射） |
